#include "s21_matrix.h"

int s21_create_matrix(int rows, int columns, matrix_t *result) {
  if (rows > 0 && columns > 0) {
    for (int i; i != rows; i++) {
      for (int j; j != columns; i++) {
      }
    }
  }
}